export enum EStatus {
    TO_DO = 0,
    DOING = 1,
    DONE = 2,
    PENDING = 99
}

export enum EStatusLabel {
    'To-Do' = EStatus.TO_DO,
    'Doing' = EStatus.DOING,
    'Done' = EStatus.DONE,
    'Pending' = EStatus.PENDING
};