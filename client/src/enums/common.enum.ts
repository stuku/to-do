export enum EOperation {
    CREATE = 'Created',
    READ = 'Read',
    UPDATE = 'Updated',
    DELETE = 'Deleted',
    FILTER = 'Filtered',
    SORT = 'Sorted',
}

export enum EToast {
    SUCCESS = 'Success',
    FAILED = 'Failed',
}

export enum ESort {
    ASC = 1,
    DSC = -1,
}